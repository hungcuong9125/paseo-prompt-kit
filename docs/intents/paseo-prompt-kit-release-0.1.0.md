Intent id: paseo-prompt-kit-release-0.1.0 — PromptKit MVP release preparation and browser QA
Repository: /Volumes/DataSSD/HomeWork/PLUGIN/paseo-prompt-kit · main
Report to: bf776d78-0b8a-44d3-9336-72a79e280ad7
Outcome: The accepted PromptKit MVP has a release-ready local state with MIT licensing, the v0.1.0 release tag created locally, and real Desktop/Web browser QA evidence from a Pi Peer using the configured MultiZen MCP. The Human receives exact push commands and their required evidence; no agent performs the blocked push.
Why now: The MVP batch is accepted and origin/main is at fcd1a7aaa0edd503058cdecdd7dcbaec0e77221d. Two local post-closeout evidence commits, d5bd749 and 09e1d89, are not on origin. The Human has now bound the license and release naming decisions and reports that Pi Peer seats can use MultiZen.
Non-goals: npm publishing; choosing an npm package name; feature expansion; changing accepted runtime behavior without a new decision; rerunning the completed bug scanner without a new evidence reason; putting credentials or MCP tokens in the repository; and claiming a public release before the Human executes and reports the push commands.
Locked decisions: License is MIT and the package metadata must identify MIT. The Git repository is paseo-prompt-kit, plugin ID is prompt-kit, display name is PromptKit, and the initial release tag is v0.1.0. npm publishing is deferred for MVP; the future package name remains undecided, with preferred @<organization>/paseo-prompt-kit and fallback paseo-prompt-kit. Browser QA uses Pi Peer on pi-peer/workbuddy/deepseek-v4.1-flash with the configured pi-mcp-adapter and MultiZen profile 20def08f-9a62-4932-9d49-f7c5ab12c6d0. Agents must not run git push or push tags from this batch; they must provide the exact commands, current SHA, and expected verification to the Human.
Invariants: Preserve the accepted code and its acceptance evidence. Keep protocol/schema version 1 and one live implementation path. Do not copy upstream code without compatible licensing and required attribution. Do not expose MULTIZEN_MCP_TOKEN or any browser credential in source, prompts, logs, screenshots, or reports. Browser claims require real successful multizen-mcp calls; unavailable MCP, login walls, CAPTCHA, or missing UI state are BLOCKED or UNKNOWN, never simulated. npm remains unpublished. A remote branch or tag is not considered updated without readable Human-run evidence.
Acceptance checks: A MIT LICENSE file and MIT package metadata are present in the release candidate, and the changes are recorded in a release evidence commit without changing accepted product behavior. A local v0.1.0 tag points to the intended release candidate. The Human receives exact push commands for main and v0.1.0, with the candidate SHA and verification commands; no agent claims the remote is updated before the Human result arrives. A Pi Peer calls the configured MultiZen tools successfully, confirms the target profile, and captures real evidence for the previously UNKNOWN Desktop/Web checks: one visible PromptKit pill/menu, reload without duplicate controls, agent switching, edit-during-request protection, and theme/readability where the profile and host permit. The QA report records each row as PASS, BLOCKED, or UNKNOWN with command/tool evidence and does not alter source in the same verification pass. Existing gate/install evidence remains traceable to the accepted tree; any changed release-only files receive the narrowest appropriate checks.
Open questions: npm publication timing and package scope remain deferred and do not block this release-preparation batch. Human execution of the push commands is external state; until its result arrives, the batch reports the release as locally prepared rather than publicly updated.
Constraints: Re-read the target-local WORKSPACE_PROTOCOL.md, framework/protocol.md, framework/provider-routing.md, MEMORY.md, the accepted packet, and the decision log before dispatch. Preserve d5bd749 and 09e1d89 as evidence; do not discard or rewrite accepted history. The project is already accepted, so this is a bounded follow-on release/QA batch; create no unrelated implementation scope. If the Pi Peer lacks the configured MCP tools in its actual seat, report BLOCKED and return the exact missing capability instead of switching silently to another route.
