Intent id: paseo-prompt-kit-post-release-composer-bug — Fix visible Composer selection regression
Repository: /Volumes/DataSSD/HomeWork/PLUGIN/paseo-prompt-kit · main
Report to: bf776d78-0b8a-44d3-9336-72a79e280ad7
Outcome: PromptKit no longer rejects a valid current Composer with the user-visible error "PromptKit needs one visible Composer." The fix is reproduced and verified in the real Paseo host without selecting or rewriting the wrong Composer, and the repository has a release-ready local bugfix candidate with exact Human handoff commands.
Why now: Human reproduced the regression after public release: the Paseo host shows a PromptKit pill, but using it produces "PromptKit needs one visible Composer." At current source 0db4c1c, client/composer/web.ts locateField filters visible [data-testid="message-input-root"] roots and fails closed unless exactly one root remains. The public v0.1.0 tag must remain immutable while this regression is investigated and corrected.
Non-goals: theme or light/dark testing; new PromptKit features; npm publishing; changing the accepted server/RPC behavior; moving or rewriting the public v0.1.0 tag; broad UI refactoring; weakening fail-closed behavior to pick an arbitrary Composer; and agent-side git push or tag push.
Locked decisions: The public v0.1.0 release is immutable. The current observed error and the supplied host screenshot are the bug evidence. MultiZen browser verification uses Pi Peer with the configured pi-mcp-adapter and profile 20def08f-9a62-4932-9d49-f7c5ab12c6d0. Browser evidence must come from real multizen-mcp calls; no auth token enters the repository, prompts, logs, or artifacts. Agents do not push or publish; they provide exact Human commands and verification. If a public fix needs a new release tag, the Lead sends a DECISION_REQUEST rather than guessing its version.
Invariants: Never rewrite a Composer that cannot be proven to belong to the selected agent/pill. Preserve the accepted prompt rewrite, settings, RPC, injection, literal, timeout, and no-auto-send behavior. Fail closed when safe Composer identity or visibility cannot be established. Keep protocol/schema version 1 and one live implementation path. Preserve all accepted history and the v0.1.0 tag. Do not mutate browser theme or unrelated profile state.
Acceptance checks: The failure reproduces against the current public release or equivalent host checkout with a recorded command, URL/profile, error, and before tree. The corrected candidate has a regression test that fails on the old behavior and passes on the fix for the observed Composer topology. Real host verification shows the PromptKit pill/menu can access the intended current Composer without the error, and rewrite/replace/focus behavior remains correct. Reload and multi-pane or hidden-Composer cases do not create duplicate controls or rewrite the wrong Composer; unsafe ambiguity remains an explicit BLOCKED/UNKNOWN result. The affected gate is green with tree-bound evidence, and the release candidate carries the bugfix without changing accepted server behavior. Human receives the exact push/tag commands and the proposed next release version; no agent claims public release before Human output.
Open questions: The release tag/version for publishing this post-release fix is undecided; the Lead sends a DECISION_REQUEST if the candidate is ready to publish. If source evidence shows Paseo 0.8.0 exposes no safe mapping from an agent pill to the active Composer, the Lead reports the smallest product decision instead of weakening the guard.
Constraints: Re-read target-local WORKSPACE_PROTOCOL.md, framework/protocol.md, framework/provider-routing.md, MEMORY.md, the accepted packet, and the release packet before dispatch. Keep the write scope bounded to the verified Composer-selection mechanism and its tests. Run no theme mutation and no broad QA while this bug is unresolved. Do not push or tag from any agent seat.
