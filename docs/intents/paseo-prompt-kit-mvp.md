Intent id: paseo-prompt-kit-mvp — PromptKit for Paseo Desktop/Web MVP
Repository: /Volumes/DataSSD/HomeWork/PLUGIN/paseo-prompt-kit · branch not fixed by Human
Report to: bf776d78-0b8a-44d3-9336-72a79e280ad7
Outcome: A usable, installable, publish-ready PromptKit MVP exists in the target repository: users can improve a coding prompt from the Paseo Desktop/Web Composer, review the rewritten text before sending, and the project is connected to https://github.com/hungcuong9125/paseo-prompt-kit with local research material excluded from published Git history while required governance records remain durable.
Why now: The implementation-ready plan at /Volumes/DataSSD/HomeWork/PLUGIN/paseo-prompt-kit/docs/IMPELEMENT_PLAN.md is available, the project is being prepared for early publication, and the repository needs its first working product path and release evidence.
Non-goals: Image, document, research, custom-action, and mobile prompt rewriting; repository-wide indexing/RAG; prompt scoring; automatic task execution; automatic sending; changing the user's primary provider/session; separate credential/API-key flows; telemetry; and publishing an npm package before its scope and release decision are settled.
Locked decisions: PromptKit is the product name, prompt-kit is the plugin id, and the MVP action is Improve coding prompt for Desktop/Web. Rewriting replaces the Composer text only; the user sends manually. The rewrite must preserve the user's intent, scope, constraint strength, language, paths, URLs, commands, code blocks, filenames, identifiers, model names, and tool names, and must not invent requirements. The implementation may use the current agent model or a user-selected dedicated model through Paseo's existing runtime. The exact GitHub remote is https://github.com/hungcuong9125/paseo-prompt-kit. The plan/reference material under docs/ and cloned upstreams/ is local agent-readable research and must not be committed or pushed; governance artifacts required by the room, including docs/intents/, docs/decision-log.md, and MEMORY.md, may be committed. When implementation Peer seats are needed, use pi-peer/workbuddy/deepseek-v4.1-flash, with thinking selected under the provider-routing contract. Browser QA may use the Human-provided MultiZen endpoint when it is available to the responsible seat; its credentials and client configuration remain outside the repository.
Invariants: Keep protocol/schema version 1 and exactly one live contract and implementation path. Do not add legacy versions, dual-read/write behavior, migrations, compatibility shims, adapters for old shapes, fallback semantics, or source-substring gates. Fail fast and fail closed. A rewrite failure, timeout, invalid model, protected-literal loss, or stale Composer edit must not overwrite the user's current text. Rewriting must not create a turn in the primary conversation, execute the supplied request, auto-send, log raw prompts by default, expose credentials, or create unbounded external data storage. Local docs/reference and upstream sources remain readable to agents in the workspace even when excluded from Git commits.
Acceptance checks: The target is a valid Git project with origin set to https://github.com/hungcuong9125/paseo-prompt-kit; the published tracked tree excludes the implementation plan/reference material and upstreams/ while allowing the required governance artifacts to be tracked, and the ignore behavior is demonstrated from the actual tree. The plugin can be installed from the local project and from its Git/package release path that the Lead binds. The PromptKit control appears once in the Desktop/Web Composer, opens a menu containing Improve coding prompt, and remains usable after reload/navigation without duplicate bindings. A non-empty coding prompt can be rewritten with the current model and with a valid dedicated model, replaces the Composer text, preserves focus, and never sends automatically or alters the primary conversation history. Empty input performs no generation; failure, timeout, invalid model, protected-literal loss, prompt injection content, or user edits during the request leave the original Composer text safe. Tests demonstrate language preservation, URL/path/command/code-block/model/tool-literal preservation, no scope expansion, temporary-generation cleanup, settings persistence and invalid-model fail-closed behavior. Typecheck, unit/integration checks, Desktop QA, Web QA, plugin reload behavior, and the release/install checks pass with evidence tied to the final tree; browser evidence is included when MultiZen is available.
Open questions: The npm package scope and whether/when to publish the npm package are not decided; the Lead sends a DECISION_REQUEST if release work depends on them. The minimum compatible Paseo version and the final branch/release tag are engineering or Human decisions only when source evidence makes them material; the Lead does not guess a user-owned release commitment.
Constraints: Read /Volumes/DataSSD/HomeWork/PLUGIN/paseo-prompt-kit/docs/IMPELEMENT_PLAN.md and every reference it names before binding engineering decisions, and keep the local upstream research available to agent models. The target is a new/uninitialized repository, so it must operate under the canonical framework contract before writable dispatch; read framework/runbooks/lead-adoption.md when the project enters adoption. Setting the GitHub remote and pushing the requested project source are allowed external effects; npm publication, release tags, or other release commitments remain subject to the open question above. Do not modify home-directory configuration or credentials from the project workflow.

---
HUMAN_DIRECTIVE (received 2026-09-21, after acceptance of pk-UkLWZ.1; amends QA/release requirements, verbatim):

1. Sau khi plugin MVP hoàn tất và bước test/QA đã đến, chạy công cụ hiện có tại /Volumes/DataSSD/HomeWork/TOOLS/ultimate_bug_scanner để quét toàn bộ dự án tìm bug/issue. Không cài đặt công cụ, không sửa source theo kết quả trong cùng lượt quét; ghi lại command, output/log path, exit code và tree SHA. Lead đánh giá findings theo acceptance trước khi closeout. Không chạy công cụ này trước khi plugin candidate sẵn sàng.

2. Browser QA có thể dùng MultiZen khi Human đã kết nối MCP client. Profile ID: 20def08f-9a62-4932-9d49-f7c5ab12c6d0. Không đưa auth token vào repository, brief, log hoặc prompt Peer. Nếu MCP chưa khả dụng, ghi rõ BLOCKED/UNKNOWN thay vì giả lập browser evidence.
